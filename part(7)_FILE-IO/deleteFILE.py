#using the OS module 
#module like code library is file writtem by another programmer that generally has a 
#function we can use....
import os
os.remove("part(7)_FILE-IO/demo.txt..") #demo.txt will be deleted